<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xgx">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".MemberManagementActivity" />
        <activity android:name=".StaffManagementActivity" />
        <activity android:name=".ProductListActivity" />
        <activity android:name=".ActivityPackageListActivity" />
        <activity android:name=".BillListActivity" />
        <activity android:name=".MemberInfoInputActivity" />
        <activity android:name=".CarInfoInputActivity" />
        <activity android:name=".MakeOrderActivity" />
        <activity android:name=".MealListActivity" />
        <activity android:name=".TechnicianListActivity" />
        <activity android:name=".MakeOrderSuccessActivity" />
        <activity android:name=".OrderPayActivity" />
        <activity android:name=".OrderDoneActivity" />
    </application>

</manifest>